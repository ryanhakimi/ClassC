(class Animal
  ()
  (init ())
  (method speak () Void
    (return (println 1))))

(class Cat Animal
  ()
  (init ()
    (super))
  (method speak () Void
    (return (println 2))))

(vardec Animal a)
(= a (new Animal))
(call a speak)

(vardec Animal pet)
(= pet (new Cat))
(call pet speak)
